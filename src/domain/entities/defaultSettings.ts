import { PluginSettings } from './PluginSettings';

export const DEFAULT_SETTINGS: PluginSettings = {
  lastReport: null,
  reports: [],
  includedSections: [], // Пользователь сам указывает нужные секции
};
